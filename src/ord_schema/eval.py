from constants import DATA_DIR, UNKNOWN_CID
import pandas as pd


FACTS_PATH = DATA_DIR / 'facts.csv'


def get_cid(smiles_table):
    """ SELECT count() from smiles_table where smiles == smiles ; """
    return UNKNOWN_CID


def extract_facts(text):
    pass
    
def load_facts_db:
    path=FACTS_PATH):
    """ loadfor row in 


def fact_check(df=FACTS) --> float:
    """ Load facts CSV and fact-check responses """
    if not isinstance(facts, pd.DataFrame):
        load_facts(facts)
